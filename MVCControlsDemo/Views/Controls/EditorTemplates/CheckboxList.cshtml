@model MVCControlsDemo.Models.CheckboxList

    @{
        ViewBag.Title = "Checkbox List";
    }

    @Html.HiddenFor(x => x.ID)
    @Html.HiddenFor(x => x.Name)

    @Html.CheckBoxFor(x => x.IsSelected)

@Html.DisplayFor(x => x.ID)
    @Html.DisplayFor(x =>  x.Name)